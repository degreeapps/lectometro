name: lectometro
description: Digital lectometer
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  bloc: ^9.0.0
  easy_localization: ^3.0.7+1
  firebase_core: ^3.12.1
  firebase_auth: 
  cloud_firestore: 
  firebase_crashlytics: 
  firebase_analytics: 
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.0

dev_dependencies:
  bloc_test: ^10.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^7.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/lang/
